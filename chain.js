const users = [{id: 1, name: 'abul', job: 'doctor'}];

// console.log(users[0]. name);

const data ={
    count: 44333,
    data : [
     
        {id1:2, name: 'ammo', job: 'leader'},

        {id4:2, name: 'ammo', job: 'leader'}
    ]
}

const firstJob = data.data[0].job;
// console.log(firstJob);

const user = {
    id: 3333,
    name: 'marup',
    address: {
        street: { 
            first: 'floor1',
            second: 'secondFloor',
            thrid: 'thridFloor'
        },
        tejgon: 'khunipara',
        rode: 22,
    }
}

const userFoolr = user.address.street.second;
console.log(userFoolr);